<% flash.each do |type, message| %>
  <% if message.present? %>
    <div class="alert <%= bootstrap_class_for(type) %> alert-dismissable">
      <button type='button' class='close' data-dismiss='alert' aria-hidden='true'>&times;</button>
      <p><%= message %></p>
    </div>
  <% end %>
<% end %>